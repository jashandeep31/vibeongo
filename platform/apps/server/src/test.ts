import dotenv from "dotenv";
dotenv.config();

export default async function test() {
  console.log("server in running");
  // const res = await createOpenRouterVirtualKey({
  //   name: "test",
  //   limit_in_dollars: 5,
  //   expires_after_in_minutes: 5,
  // });
  // console.log(res);
  //
}
