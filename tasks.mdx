

### Vibe on go 

1. web ui ask the user  (web-app)
   1. user can create account 
   2. user can setup the new dev server asksk for: github, databases, .env port users wanan to expore , telegram bot token
   3. user can get the server dev from bot but now we have to add .env auto matciliy

2. scripts  run to create the server (infra-scripts )
  1. setup the server
  2. re wakeup the server


3. internal tool run in the vps (core)
  1. run in the user server installed things
  2. show the live website with cloudflare
  3. run the scripts works with bot
